---
jenkins_agent_server_url: ""         # REQUIRED: URL of the server to register with (caller-supplied)
jenkins_agent_work_dir: "/var/lib/jenkins-agent"
jenkins_agent_secret: ""             # caller-supplied join secret; never retrieved by this role
